{
  "name": "keymaster-ui-api",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "eslint": "^7.14.0",
    "express": "^4.17.1",
    "prettier": "^2.2.0"
  },
  "scripts": {
    "start": "set -a; source .env; set -a && node ./api.js"
  }
}
